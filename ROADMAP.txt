babel/
├── src/
│   ├── main.rs
│   ├── cli.rs
│   ├── cmd.rs
│   ├── cmd/
│   │   ├── mod.rs
│   │   ├── construct.rs
│   │   ├── compose.rs
│   │   ├── display.rs
│   │   ├── embed.rs
│   │   └── interpret.rs
│   ├── edn.rs
│   ├── lookup.rs
│   └── util.rs
├── config/
│   ├── display_binding.toml
│   ├── display_trigger.toml
│   ├── embed.toml
│   └── interpret.toml
└── Cargo.toml



babel tool read custom EDN‑based keybinding format
- display => bindings table
- interpret => bindings into program‑specific config
- embed => bindings into existing config files
- construct => initiate babel self-config
- compose => create templates
- completion => shell completions
- identity => reveal identity

strengths:
- unified, cross‑program keybinding management from single source of truth
- modular CLI with clear subcommands & global filtering
- external TOML lookups make display / output formatting customisable
- embedding via issac allows precise in‑place modifications of existing configs
- coloured table output improves readability

weaknesses:

usefulness:
babel is very useful for advanced terminal users who maintain custom keybinding layers and want to automatically synchronise consistent hotkeys across programs, saving repetitive manual config editing

potential enhancements:
* Marker‑based embedding
* Data‑driven formatting via TOML templates
* dry‑run / diff preview => show what would change before embedding
* robust EDN parsing:
  * replace custom regex extraction with a schema‑driven approach
  * add schema validation via serde
  * error reporting => include file name and line number on parse failures
  * add validate edn command
* improve error handling
- centralised transformation maps => eliminate duplication & allow pluggable formats
- configurable embed paths => use TOML profiles instead of hard‑coded relative paths
- add test coverage for parsing, formatting & embedding logic



- add a command for generating documentation on sql architecture => fetch table name, get table fields & comments
- wire up sql generation command with lsp

- render binds through babel can be implemented also to other programs => yazi & lazygit?
- render a color coded keyboard
  for empty (dimmed) & used bindings (colored & bold)
  add tab completion for bindable modifiers => right-alt, right-cmd, left-shift, etc



==================================================
==================================================
